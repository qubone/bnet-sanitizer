# Battle.net Sanitizer

Pre-flight maintenance tool designed to prevent `VK_ERROR_DEVICE_LOST` crashes, agent locks, and Wayland window rendering bugs when running Battle.net under Lutris / Wine.

## Installation via `uv`

```bash
cd bnet-sanitizer
uv tool install .